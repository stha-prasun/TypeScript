const superHeros: string[] = []; //The array should be of a type of string array

const powerLevel: Array<number> = []; //Another syntax of array

superHeros.push("Ironman");

powerLevel.push(6);

//An array of type User
type User = {
  name: string;
  email: string;
};

const allUsers: User[] = [];

//2D Array
const MLModels: number[][] = [
  [255, 255, 255],
  [255, 255, 255],
];
